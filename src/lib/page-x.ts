/** Horizontal inset that matches the header logo and nav links. */
export const PAGE_X = "px-4 md:px-6";

/** Vertical inset used by main and last-section copy. */
export const PAGE_Y = "py-8";

/** Pull a full-bleed block out to the site frame, then put PAGE_X back on inner copy. */
export const PAGE_X_BLEED = "-mx-4 md:-mx-6";
